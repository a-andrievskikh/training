// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(num) {
  num = fixedValue;
  num++;
  return num;

  // Only change code above this line
}
